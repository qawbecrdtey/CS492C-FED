import React from 'react';
import Row from './Row';

export default {
    title : 'Row component',
    component: Row,
};

export const RowComponent = () => <Row />;

RowComponent.story = {
    name: 'Row',
};